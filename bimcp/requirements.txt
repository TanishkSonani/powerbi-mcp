 # Core MCP
    mcp[cli]>=1.0.0

    # Data validation
    pydantic>=2.0

    # YAML frontmatter parsing (resource .md files)
    pyyaml>=6.0

    # Process inspection (Phase 3 port discovery fallback)
    psutil>=5.9

    # Optional env config
    python-dotenv>=1.0

    # Phase 2 — .NET interop for TOM + TmdlSerializer
    pythonnet>=3.0.3

    # Testing
    pytest>=7.0
    pytest-asyncio>=0.21